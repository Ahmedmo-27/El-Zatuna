/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!*****************************************************************!*\
  !*** ./resources/js/design_1/parts/become_instructor_wizard.js ***!
  \*****************************************************************/
function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
function _iterableToArrayLimit(arr, i) { var _i = null == arr ? null : "undefined" != typeof Symbol && arr[Symbol.iterator] || arr["@@iterator"]; if (null != _i) { var _s, _e, _x, _r, _arr = [], _n = !0, _d = !1; try { if (_x = (_i = _i.call(arr)).next, 0 === i) { if (Object(_i) !== _i) return; _n = !1; } else for (; !(_n = (_s = _x.call(_i)).done) && (_arr.push(_s.value), _arr.length !== i); _n = !0); } catch (err) { _d = !0, _e = err; } finally { try { if (!_n && null != _i["return"] && (_r = _i["return"](), Object(_r) !== _r)) return; } finally { if (_d) throw _e; } } return _arr; } }
function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }
(function ($) {
  "use strict";

  $('body').on('change', '.js-user-bank-input', function (e) {
    e.preventDefault();
    var $optionSelected = $(this).find("option:selected");
    var specifications = $optionSelected.attr('data-specifications');
    var $card = $('.js-bank-specifications-card');
    var html = '';
    if (specifications) {
      Object.entries(JSON.parse(specifications)).forEach(function (_ref, key) {
        var _ref2 = _slicedToArray(_ref, 2),
          index = _ref2[0],
          item = _ref2[1];
        html += '<div class="form-group">\n' + '         <label class="form-group-label bg-white">' + item + '</label>\n' + '         <input type="text" name="bank_specifications[' + index + ']" value="" class="form-control bg-white"/>\n' + ' </div>';
      });
    }
    $card.html(html);
  });
  $('body').on('change', 'input[name="role"]', function () {
    var value = $(this).val();
    var type = value === "teacher" ? "become_instructor" : "become_organization";
    $.post('/become-instructor/form-fields', {
      type: type
    }, function (result) {
      if (result) {
        $('.js-form-fields-card').html(result.html);
        formsDatetimepicker();
      }
    });
  });

  // Occupations/subjects: text field with search + always-visible "Add different subject" option
  function initOccupationsInput() {
    var $wrapper = $('.js-occupations-wrapper');
    if (!$wrapper.length) return;
    var $input = $wrapper.find('.js-occupations-input');
    var $dropdown = $wrapper.find('.js-occupations-dropdown');
    var $results = $wrapper.find('.js-occupations-results');
    var $addNew = $wrapper.find('.js-occupations-add-new');
    var $addNewTerm = $wrapper.find('.js-add-new-term');
    var $tags = $wrapper.find('.js-occupations-tags');
    var $hiddenContainer = $wrapper.find('.js-occupations-hidden-container');
    var $loading = $wrapper.find('.js-occupations-loading');
    var $error = $wrapper.find('.js-occupations-error');
    var selected = [];
    var searchTimeout = null;
    var hideDropdownTimeout = null;
    var lastSelectedAt = 0;
    var lastAddNewAt = 0;
    function setLoading(isLoading) {
      if (isLoading) {
        $loading.removeClass('d-none');
        $input.prop('disabled', true);
        $addNew.addClass('disabled');
      } else {
        $loading.addClass('d-none');
        $input.prop('disabled', false);
        $addNew.removeClass('disabled');
      }
    }
    function showError(message) {
      if (!message) {
        $error.addClass('d-none').text('');
        return;
      }
      $error.removeClass('d-none').text(message);
    }
    function syncHiddenInputs() {
      $hiddenContainer.empty();
      selected.forEach(function (item) {
        $hiddenContainer.append($('<input>').attr({
          type: 'hidden',
          name: 'occupations[]',
          value: item.id
        }));
      });
    }
    function addSelected(item) {
      if (selected.some(function (s) {
        return String(s.id) === String(item.id);
      })) return;
      selected.push(item);
      renderTags();
      syncHiddenInputs();
    }
    function removeSelected(id) {
      selected = selected.filter(function (s) {
        return String(s.id) !== String(id);
      });
      renderTags();
      syncHiddenInputs();
    }
    function renderTags() {
      $tags.empty();
      selected.forEach(function (item) {
        var $tag = $('<span class="badge bg-[#F5F9E8] text-[#072923] px-3 py-1 rounded-8 d-inline-flex align-items-center gap-1">' + '<span>' + $('<div>').text(item.text).html() + '</span>' + '<button type="button" class="js-remove-tag btn btn-link p-0 text-[#072923]/60 hover:text-danger" style="font-size: 14px; line-height: 1;" data-id="' + item.id + '">&times;</button>' + '</span>');
        $tags.append($tag);
      });
    }
    function doSearch(q) {
      showError('');
      setLoading(true);
      $addNewTerm.text(q || 'type above first');
      $addNew.removeClass('d-none');
      $.get('/become-instructor/search-subjects', {
        q: q || ''
      }).done(function (data) {
        var results = data.results || [];
        $results.empty();
        if (results.length === 0) {
          if (q) {
            $results.append($('<div class="p-2 text-[#072923]/50">No matching subjects. Use "Add different subject" below to add "' + $('<div>').text(q).html() + '".</div>'));
          } else {
            $results.append($('<div class="p-2 text-[#072923]/50">No subjects found.</div>'));
          }
        } else {
          results.forEach(function (item) {
            var textEsc = $('<div>').text(item.text).html();
            var $row = $('<div class="js-result-row p-2 rounded-8 cursor-pointer hover:bg-[#F5F9E8]/50" style="min-height: 44px; display: flex; align-items: center;" data-id="' + item.id + '">' + textEsc + '</div>');
            $results.append($row);
          });
        }
      }).fail(function () {
        showError('Could not load subjects. Please check your connection and try again.');
      }).always(function () {
        setLoading(false);
      });
    }
    function showDropdown() {
      $dropdown.removeClass('d-none');
    }
    function hideDropdown() {
      if (hideDropdownTimeout) clearTimeout(hideDropdownTimeout);
      hideDropdownTimeout = setTimeout(function () {
        hideDropdownTimeout = null;
        $dropdown.addClass('d-none');
      }, 280);
    }
    function cancelHideDropdown() {
      if (hideDropdownTimeout) {
        clearTimeout(hideDropdownTimeout);
        hideDropdownTimeout = null;
      }
    }
    $dropdown.on('mousedown touchstart', function () {
      cancelHideDropdown();
    });
    $input.on('focus', function () {
      cancelHideDropdown();
      var q = $.trim($input.val());
      showDropdown();
      doSearch(q);
    });
    $input.on('input', function () {
      var q = $.trim($input.val());
      clearTimeout(searchTimeout);
      searchTimeout = setTimeout(function () {
        doSearch(q);
        if (q) showDropdown();
      }, 200);
    });
    $input.on('blur', function () {
      hideDropdown();
    });
    function selectResultRow($row) {
      var id = $row.data('id');
      var text = $row.text().trim();
      if (!id && text === '') return;
      if (Date.now() - lastSelectedAt < 400) return;
      lastSelectedAt = Date.now();
      setLoading(true);
      addSelected({
        id: id,
        text: text
      });
      $input.val('');
      hideDropdown();
      $input.focus();
      setTimeout(function () {
        setLoading(false);
      }, 250);
    }
    $results.on('click', '.js-result-row', function (e) {
      e.preventDefault();
      selectResultRow($(this));
    });
    $results.on('touchend', '.js-result-row', function (e) {
      e.preventDefault();
      selectResultRow($(this));
    });
    function handleAddNewSubject() {
      if (Date.now() - lastAddNewAt < 400) return;
      var term = $.trim($input.val()) || $.trim($addNewTerm.text());
      if (!term || term === 'type above first') return;
      lastAddNewAt = Date.now();
      showError('');
      setLoading(true);
      var token = $('meta[name="csrf-token"]').attr('content') || $('input[name="_token"]').val();
      $.post('/become-instructor/create-subject', {
        title: term,
        _token: token
      }, function (data) {
        addSelected({
          id: data.id,
          text: data.text || term
        });
        $input.val('');
        hideDropdown();
        $input.focus();
      }).fail(function () {
        addSelected({
          id: term,
          text: term
        });
        showError('Subject was added locally, but we could not save it on the server. Please try again later.');
        $input.val('');
        hideDropdown();
      }).always(function () {
        setLoading(false);
      });
    }
    $addNew.on('click', function (e) {
      e.preventDefault();
      handleAddNewSubject();
    }).on('touchend', function (e) {
      e.preventDefault();
      handleAddNewSubject();
    });
    $tags.on('click', '.js-remove-tag', function (e) {
      e.preventDefault();
      removeSelected($(this).data('id'));
    });
    $wrapper.on('click', function (e) {
      if ($(e.target).closest('.js-occupations-input').length || $(e.target).closest('.js-occupations-dropdown').length) return;
      if ($(e.target).closest('.js-occupations-tags').length) {
        hideDropdown();
        return;
      }
      $input.focus();
    });
    var $tabPane = $wrapper.closest('.tab-pane');
    if ($tabPane.length && $tabPane.attr('id')) {
      var tabId = $tabPane.attr('id');
      $(document).on('shown.bs.tab', 'a[href="#' + tabId + '"], a[data-bs-target="#' + tabId + '"], [data-toggle="tab"][href="#' + tabId + '"]', function () {
        var $w = $('#' + tabId).find('.js-occupations-wrapper');
        if ($w.length) $w.find('.js-occupations-input').trigger('focus');
      });
    }

    // Initial selected from server
    try {
      var initial = $wrapper.data('initial') || [];
      if (Array.isArray(initial) && initial.length) {
        initial.forEach(function (item) {
          addSelected({
            id: item.id,
            text: item.text
          });
        });
      }
    } catch (err) {}
    syncHiddenInputs();
  }
  $(document).ready(function () {
    initOccupationsInput();
  });
})(jQuery);
/******/ })()
;