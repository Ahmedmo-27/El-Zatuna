/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!*******************************************************!*\
  !*** ./resources/js/design_1/panel/file-drag-drop.js ***!
  \*******************************************************/
function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }
function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, _toPropertyKey(descriptor.key), descriptor); } }
function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return _typeof(key) === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (_typeof(input) !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (_typeof(res) !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
/**
 * File Drag and Drop Functionality
 * Provides accessible drag and drop file upload with keyboard support
 */

(function ($) {
  'use strict';

  var FileDragDrop = /*#__PURE__*/function () {
    function FileDragDrop($zone) {
      _classCallCheck(this, FileDragDrop);
      this.$zone = $zone;
      this.$fileInput = null;
      this.$overlay = $zone.find('.js-drag-drop-overlay');
      this.$content = $zone.find('.js-drag-drop-content');
      this.$selectedFileDisplay = $zone.closest('.form-group').find('.js-selected-file-display');
      this.$fileName = this.$selectedFileDisplay.find('.js-selected-file-name');
      this.$fileSize = this.$selectedFileDisplay.find('.js-selected-file-size');
      this.$removeBtn = this.$selectedFileDisplay.find('.js-remove-file');
      this.init();
    }
    _createClass(FileDragDrop, [{
      key: "init",
      value: function init() {
        var fileInputId = this.$zone.data('file-input-id');
        this.$fileInput = $("#".concat(fileInputId));
        if (!this.$fileInput.length) {
          console.warn('File input not found:', fileInputId);
          return;
        }
        this.bindEvents();
      }
    }, {
      key: "bindEvents",
      value: function bindEvents() {
        var _this = this;
        // Drag and drop events
        this.$zone.on('dragover', function (e) {
          return _this.handleDragOver(e);
        });
        this.$zone.on('dragleave', function (e) {
          return _this.handleDragLeave(e);
        });
        this.$zone.on('drop', function (e) {
          return _this.handleDrop(e);
        });

        // Click to upload
        this.$zone.on('click', function (e) {
          // Only trigger if clicking the zone itself, not child elements
          if (e.target === _this.$zone[0] || $(e.target).closest('.js-drag-drop-content').length) {
            e.preventDefault();
            _this.$fileInput.trigger('click');
          }
        });

        // Keyboard support (Enter/Space to activate)
        this.$zone.on('keydown', function (e) {
          if (e.key === 'Enter' || e.key === ' ') {
            e.preventDefault();
            _this.$fileInput.trigger('click');
          }
        });

        // File input change
        this.$fileInput.on('change', function (e) {
          return _this.handleFileSelect(e);
        });

        // Remove file button
        this.$removeBtn.on('click', function (e) {
          e.preventDefault();
          e.stopPropagation();
          _this.removeFile();
        });

        // Update custom file label when file is selected
        this.$fileInput.on('change', function () {
          var _this$$fileInput$0$fi;
          var fileName = ((_this$$fileInput$0$fi = _this.$fileInput[0].files[0]) === null || _this$$fileInput$0$fi === void 0 ? void 0 : _this$$fileInput$0$fi.name) || '';
          if (fileName) {
            _this.$fileInput.closest('.custom-file').find('.custom-file-label').text(fileName);
          }
        });
      }
    }, {
      key: "handleDragOver",
      value: function handleDragOver(e) {
        e.preventDefault();
        e.stopPropagation();
        this.$zone.addClass('drag-over');
        this.$overlay.removeClass('d-none');
      }
    }, {
      key: "handleDragLeave",
      value: function handleDragLeave(e) {
        e.preventDefault();
        e.stopPropagation();

        // Only remove overlay if we're leaving the zone itself
        if (!this.$zone[0].contains(e.relatedTarget)) {
          this.$zone.removeClass('drag-over');
          this.$overlay.addClass('d-none');
        }
      }
    }, {
      key: "handleDrop",
      value: function handleDrop(e) {
        var _e$originalEvent$data;
        e.preventDefault();
        e.stopPropagation();
        this.$zone.removeClass('drag-over');
        this.$overlay.addClass('d-none');
        var files = (_e$originalEvent$data = e.originalEvent.dataTransfer) === null || _e$originalEvent$data === void 0 ? void 0 : _e$originalEvent$data.files;
        if (files && files.length > 0) {
          this.processFiles(files);
        }
      }
    }, {
      key: "handleFileSelect",
      value: function handleFileSelect(e) {
        var files = e.target.files;
        if (files && files.length > 0) {
          this.processFiles(files);
        }
      }
    }, {
      key: "processFiles",
      value: function processFiles(files) {
        var file = files[0];

        // Validate file size (2GB = 2147483648 bytes)
        var maxSize = 2147483648;
        if (file.size > maxSize) {
          this.showError('File size exceeds 2GB limit');
          return;
        }

        // Validate file type
        var allowedTypes = ['video/mp4', 'video/avi', 'video/x-msvideo', 'video/x-matroska', 'video/quicktime', 'video/x-ms-wmv', 'video/webm', 'application/pdf', 'application/msword', 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', 'text/plain'];
        var fileExtension = file.name.split('.').pop().toLowerCase();
        var allowedExtensions = ['mp4', 'avi', 'mkv', 'mov', 'wmv', 'flv', 'webm', 'pdf', 'doc', 'docx', 'txt'];
        if (!allowedExtensions.includes(fileExtension)) {
          this.showError('File type not supported. Please upload MP4, AVI, MKV, MOV, PDF, DOC, DOCX, or TXT files.');
          return;
        }

        // Set file to input
        var dataTransfer = new DataTransfer();
        dataTransfer.items.add(file);
        this.$fileInput[0].files = dataTransfer.files;

        // Update display
        this.displaySelectedFile(file);

        // Trigger change event for any listeners
        this.$fileInput.trigger('change');
      }
    }, {
      key: "displaySelectedFile",
      value: function displaySelectedFile(file) {
        var fileName = file.name;
        var fileSize = this.formatFileSize(file.size);
        this.$fileName.text(fileName);
        this.$fileSize.text(fileSize);
        this.$selectedFileDisplay.removeClass('d-none');

        // Update custom file label
        var $customLabel = this.$fileInput.closest('.custom-file').find('.custom-file-label');
        if ($customLabel.length) {
          $customLabel.text(fileName);
        }
      }
    }, {
      key: "removeFile",
      value: function removeFile() {
        // Clear file input
        this.$fileInput.val('');

        // Clear custom file label
        var $customLabel = this.$fileInput.closest('.custom-file').find('.custom-file-label');
        if ($customLabel.length) {
          $customLabel.text($customLabel.data('original-text') || 'Choose file');
        }

        // Hide selected file display
        this.$selectedFileDisplay.addClass('d-none');

        // Trigger change event
        this.$fileInput.trigger('change');
      }
    }, {
      key: "formatFileSize",
      value: function formatFileSize(bytes) {
        if (bytes === 0) return '0 Bytes';
        var k = 1024;
        var sizes = ['Bytes', 'KB', 'MB', 'GB'];
        var i = Math.floor(Math.log(bytes) / Math.log(k));
        return Math.round(bytes / Math.pow(k, i) * 100) / 100 + ' ' + sizes[i];
      }
    }, {
      key: "showError",
      value: function showError(message) {
        // Remove any existing error messages
        this.$zone.closest('.form-group').find('.invalid-feedback').remove();

        // Show error message
        var $error = $('<div class="invalid-feedback d-block">' + message + '</div>');
        this.$zone.closest('.form-group').append($error);

        // Remove error after 5 seconds
        setTimeout(function () {
          $error.fadeOut(function () {
            return $error.remove();
          });
        }, 5000);
      }
    }]);
    return FileDragDrop;
  }(); // Make FileDragDrop available globally
  window.FileDragDrop = FileDragDrop;

  // Initialize on document ready
  $(document).ready(function () {
    // Initialize for all drag drop zones
    $('.js-file-drag-drop-zone').each(function () {
      if (!$(this).data('drag-drop-initialized')) {
        new FileDragDrop($(this));
        $(this).data('drag-drop-initialized', true);
      }
    });

    // Re-initialize when new zones are added dynamically (using MutationObserver for better performance)
    if (typeof MutationObserver !== 'undefined') {
      var observer = new MutationObserver(function (mutations) {
        mutations.forEach(function (mutation) {
          mutation.addedNodes.forEach(function (node) {
            if (node.nodeType === 1) {
              // Element node
              var $node = $(node);
              var $zones = $node.find('.js-file-drag-drop-zone').addBack('.js-file-drag-drop-zone');
              $zones.each(function () {
                if (!$(this).data('drag-drop-initialized')) {
                  new FileDragDrop($(this));
                  $(this).data('drag-drop-initialized', true);
                }
              });
            }
          });
        });
      });
      observer.observe(document.body, {
        childList: true,
        subtree: true
      });
    }
  });

  // Also initialize when storage type changes (for dynamic forms)
  $(document).on('change', '.js-file-storage', function () {
    var $form = $(this).closest('form, .js-content-form');
    var $uploadZone = $form.find('.js-file-drag-drop-zone');
    if ($uploadZone.length && !$uploadZone.data('drag-drop-initialized')) {
      new FileDragDrop($uploadZone);
      $uploadZone.data('drag-drop-initialized', true);
    }
  });
})(jQuery);
/******/ })()
;